# 「タグ付け」「検索強化」タスク一覧

- [x] `app.py` の修正
  - [x] `init_db` で `tags` カラムの暗黙的追加 (ALTER TABLE) の実装
  - [x] `POST /api/records` にて `tags` データの受け取りとデータベースへの保存
  - [x] `GET /api/records` にて `tags` の取得およびLIKE検索の対象に付加
  - [x] `GET /api/records/export` (CSV, JSON, TXT) に `tags` 項目を反映
- [x] タイル・レイアウト (`index.html`) の修正
  - [x] 入力フォームに「おすすめタグ」と「カスタムタグ」のUI追加
  - [x] 検索バー付近のUI調整 (必要な場合)
- [x] フロントエンド・ロジック (`app.js`) の修正
  - [x] 選択/入力されたタグ情報をPOSTリクエストに追加
  - [x] 検索・表示処理にて、カード内にタグのバッジを描画
  - [x] タグバッジクリック時に、検索窓にタグ名を入れて自動検索する機能の追加
- [x] CSS (`style.css`) の修正
  - [x] おすすめタグ・タグバッジ・選択状態等のスタイル定義追加
